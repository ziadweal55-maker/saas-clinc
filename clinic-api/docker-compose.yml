version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: clinic-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: clinic_password_992
      POSTGRES_DB: clinic_saas
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

volumes:
  postgres_data:
